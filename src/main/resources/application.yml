application:
  title: ${APPLICATION_TITLE:OpenDataJungle Knowledge API}
  version: ${APPLICATION_VERSION:@project.version@}

spring:
  application:
    name: OpenDataJungle Knowledge API
  servlet:
    multipart:
      max-file-size: ${MULTIPART_MAX_FILE_SIZE:5MB}
      max-request-size: ${MULTIPART_MAX_REQUEST_SIZE:10MB}

  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/open_data_jungle}
    username: ${DATABASE_USERNAME:user}
    password: ${DATABASE_PASSWORD:password}
    driver-class-name: ${DATABASE_DRIVER:org.postgresql.Driver}
    hikari:
      connection-init-sql: ${DATABASE_CONNECTION_INIT_SQL:SET search_path TO knowledge, public}
  jpa:
    hibernate:
      ddl-auto: ${JPA_DDL_AUTO:none}
    show-sql: ${JPA_SHOW_SQL:false}
    properties:
      hibernate:
        default_schema: ${HIBERNATE_DEFAULT_SCHEMA:knowledge}
        format_sql: ${HIBERNATE_FORMAT_SQL:false}
    open-in-view: ${JPA_OPEN_IN_VIEW:false}
  ai:
    vectorstore:
      pgvector:
        schema-name: ${PGVECTOR_SCHEMA_NAME:knowledge}
        index-type: ${PGVECTOR_INDEX_TYPE:HNSW}
        distance-type: ${PGVECTOR_DISTANCE_TYPE:COSINE_DISTANCE}
        dimensions: ${PGVECTOR_DIMENSIONS:1536}
        max-document-batch-size: ${PGVECTOR_MAX_DOCUMENT_BATCH_SIZE:10000}

security:
  admin-role: ${SECURITY_ADMIN_ROLE:resource.admin}
  not-affectable-roles: ${SECURITY_NOT_AFFECTABLE_ROLES:resource.admin,search.semantic}
  scopes:
    search:
      semantic: ${SECURITY_SCOPE_SEARCH_SEMANTIC:search.semantic}
    resources:
      read: ${SECURITY_SCOPE_RESOURCES_READ:resources.read}
      write: ${SECURITY_SCOPE_RESOURCES_WRITE:resources.write}
      delete: ${SECURITY_SCOPE_RESOURCES_DELETE:resources.delete}
    folders:
      read: ${SECURITY_SCOPE_FOLDERS_READ:folders.read}
      write: ${SECURITY_SCOPE_FOLDERS_WRITE:folders.write}
      delete: ${SECURITY_SCOPE_FOLDERS_DELETE:folders.delete}

http:
  client:
    connect-timeout-seconds: ${HTTP_CLIENT_CONNECT_TIMEOUT:5}
    read-timeout-seconds: ${HTTP_CLIENT_READ_TIMEOUT:10}

content:
  download:
    timeout-seconds: ${CONTENT_DOWNLOAD_TIMEOUT:30}
    connect-timeout-seconds: ${CONTENT_DOWNLOAD_CONNECT_TIMEOUT:10}
    max-size-bytes: ${CONTENT_DOWNLOAD_MAX_SIZE_BYTES:5242880}
    block-internal-networks: ${CONTENT_DOWNLOAD_BLOCK_INTERNAL_NETWORKS:true}
    allowed-hosts: ${CONTENT_DOWNLOAD_ALLOWED_HOSTS:}

embedding:
  # provider : OPENAI (default) | OLLAMA
  provider: ${EMBEDDING_PROVIDER:OPENAI}
  api-key: ${EMBEDDING_API_KEY:demo}
  base-url: ${EMBEDDING_BASE_URL:http://localhost:11434}
  model: ${EMBEDDING_MODEL:text-embedding-3-small}
  dimensions: ${EMBEDDING_DIMENSIONS:}

reranker:
  type: ${RERANKER_TYPE:}
  base-url: ${RERANKER_BASE_URL:http://localhost:8085}

server:
  port: ${SERVER_PORT:8081}

open-data-jungle:
  reference-data-api:
    base-url: ${OPEN_DATA_JUNGLE_REFERENCE_DATA_API_BASE_URL:http://localhost:8083/api/v1}
    user-groups-by-user-id: ${OPEN_DATA_JUNGLE_REFERENCE_DATA_API_USER_GROUPS:/users/{userId}/groups}
    user-by-username: ${OPEN_DATA_JUNGLE_REFERENCE_DATA_API_USER_BY_NAME:/users/username/{username}}
